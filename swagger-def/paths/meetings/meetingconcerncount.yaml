get:
  operationId: getMeetingconcernCount
  tags:
    - Lists
  summary: Return the count of meetingconcerns available to the current user
  parameters:
    - $ref: '#/parameters/tokenParam'
    - $ref: '#/parameters/requesterParam'
    - $ref: '#/parameters/dateFormatParam'
    - $ref: '#/parameters/filterParam'
  description: ''
  responses:
    '200':
      description: >-
        Successful operation returning the number of meetingconcerns
        corresponding to the queries parameters
      schema:
        type: number
